# 全專案潛在 Bug 稽核（2026-07-09）

> 來源：四個平行分析代理各自靜態稽核（權限 / 進度甘特延期 / 報告通知流程 / 一般正確性），去重後整理。
> **未修改任何程式碼**，僅供逐條確認。

## 如何閱讀

每一項標注「判定類型」：

- 🔧 **程式事實** — 純看程式碼即可確認是 bug（不需要業務判斷），改了就對。
- 🔀 **需對照流程/需求** — 是不是 bug 取決於「你要的流程本來就長怎樣」，要你確認後才動。
- ⚠️ **受保護區** — 延期 cascade 相關，約束為「不可移除 cascade」，僅在你點頭後才碰。

嚴重度：🔴 P0（功能實際壞掉）/ 🟠 P1（正確性或顯示不一致）/ 🟡 P2（防禦/清理）。

---

## 📊 進度總覽（2026-07-10 更新）

### ✅ 已修
- **#1~#17 全數處理完**：#1 撤回、#2 通知移對路徑、#3 cron 催繳改對人+開案把關、#4 里程碑進度雙來源、#5 延期連動（重做）、#6 三套「今天」統一、#7 reportOnly 作廢、#8 weekOf 三步貫通、#9 有紀錄就有進度、#10 父任務有%沒條、#11 延期紅段 original、#12 A 同日防呆、#13 側邊欄徽章改 DB、#14 safeJsonParse、#15 通知去重（新 record_uploaded 型別+當日窗）、#16 決策=維持現狀、#17 逾期通知移出 GET→cron。
- **延伸修正（原清單沒有，這串做的）**：
  - 甘特**深層子任務不畫紅延長段** → 修（`renderGanttSub` 補延長邏輯）。
  - 延期核准後**祖先(頂層)被 step-4 用舊工期蓋掉、里程碑不紅** → 修（composer 送延期時祖先連 `durationDays` 一起延長）。
  - **S 審核「延期影響」樹狀視覺**（灰=原定/紅=延長/琥珀=順延，含路徑+負責人）→ 新做。
  - **撰寫本週報告列出預計起訖日**（含延期後）→ 新做。
  - **編輯專案 vs 甘特日期不一致** → 修（DB 為準：任務工期由 DB 日期反推、里程碑起點取最早任務）。
  - 延期送出後外層不刷新、延期待審核 badge/下拉標示、逾期硬擋報告改軟提醒 → 修。

### ⏳ 待你決策（🔀）
- **#P8** 更新紀錄篩選依 log author 還是 task assignee（僅剩這一題）。
- （#1~#17 全數決策/修好。）

### 🧭 修復策略（客戶確認）
- **先修「操作流程上明顯看得到」的 bug**（進度/甘特顯示、崩潰）→ 讓 USER 沒聲音。
- **權限（#S1~S5）、P2 清理、reportOnly 死碼 → 延後**：USER 流程還在反覆，現在動底層/架構容易白工，且前面雜音會干擾判斷。等流程穩定再處理。

### 🔧 待修（純技術、零爭議）
- **#1~#17 全部處理完**（修好或決策已定）。剩 **P2**：#P1 身分 by-name、#P2 通知未 catch、#P3 rollup 無交易、#P4 死碼、#P5~P7 甘特/週次時區、#P8（篩選 author/assignee，待決策）。

### 🔒 權限（架構級大決策，非零散改）
- **#S1** 4 支裸 admin API 補 guard【建議優先、低風險】；**#S2~S5** mock auth 無 server 驗證（需 session/middleware）。

---

## 🔴 P0 — 功能實際上是壞的（靜默失效）

### #1 ~~R 的工作紀錄進不了「更新紀錄」（publish 斷鏈）~~ ✅ 已撤回（不是 bug）
- **結論（2026-07-09 與需求方確認）**：這是**正確設計，不是 bug**。「更新紀錄」（`weekly-activity-summary.tsx`）本來就**以 A 送出（發布）的報告為主**；R 的報告是給 A 看/挑/匯入的**原料**，不該自己跑進更新紀錄。
- **已驗證流程是通的**：R 填寫週報（`task-logs/batch` → TaskLog，無 `publishedAt`）→ A 撰寫台「查看 R 報告」讀 `project.taskLogs`（`a-weekly-report-composer.tsx:105`，**讀全部 log、不分 publishedAt**）→ A 看得到、可「匯入我的報告」→ A 編修後送出（`weekly-report` POST 設 `publishedAt`）→ 更新紀錄顯示 A 這筆。整條迴圈正常。
- **原誤解**：我把「R 不在更新紀錄」誤讀成斷鏈，實際是預期行為。

### #2 R 送出用了兩套不相通的資料模型 ✅ 已收斂（2026-07-09）
- **結論**：R 正式路徑是「填寫週報彈窗」→ `task-logs/batch`（TaskLog），A 看得到。`member-weekly-reports`（MemberWeeklyReport）是**已隱藏的舊入口**（「我的任務週報 (MY)」，因客戶需求反覆而下架），A 端看不到、已無實際流量。
- **已處理**：把「R→A 通知」從死路 `member-weekly-reports` POST **移到** `task-logs/batch` POST（R 真正送出處）。→ 現在 R 送週報，A 會收到通知。
- **殘留（可選）**：`member-weekly-reports` route、`MemberWeeklyReport` 表、`handleSubmitWeeklyReport`（`my-tasks/page.tsx:748`）屬死碼，可納入清理（見文末「死碼清理」）。

<details><summary>原始發現（保留備查）</summary>
- **位置**：`app/api/member-weekly-reports/route.ts`（寫 `MemberWeeklyReport`）vs `app/my-tasks/page.tsx:1359`（走 `task-logs/batch` 寫 `TaskLog`）
- **現象**：R 的送出實際有三條路徑：
  - `page.tsx:756` → `/api/member-weekly-reports`（「我的任務週報 MY」，寫 MemberWeeklyReport）
  - `page.tsx:1359` → `/api/projects/{id}/task-logs/batch`（R 週報對話框，有日期的紀錄，寫 TaskLog）
  - `page.tsx:1452` → `/api/projects/{id}/task-logs`（單筆，寫 TaskLog）
  - A 端審視/更新紀錄/彙整**只讀 TaskLog**；`MemberWeeklyReport` 這條 A 完全看不到。
- **後果**：R 走 MemberWeeklyReport 那條等於寫了沒人看；而我近期加的「R→A 通知」剛好掛在 MemberWeeklyReport POST（次要路徑），真正餵更新紀錄的 `task-logs/*` 那兩條反而不通知。
- **需你確認的流程**：這兩套是刻意並存（MY 週報 vs 任務紀錄用途不同）還是歷史遺留？要不要統一到 TaskLog。
- **建議**：統一到 TaskLog；`task-logs/batch` 成功後補呼叫 `notifyRecordUploadedToAccountable`；或廢除 MemberWeeklyReport 這條。
</details>

### #3 Cron 催繳查了一張沒人在寫的表 ✅ 已修（2026-07-09）
- **已處理**：`weekly-notification` 移除查空表 `weeklyUpdate.count`；改成「找出**有進行中任務（未完成、未回報無後續、已開始）卻本週沒寫 TaskLog** 的 R，逐一提醒該 R 本人（站內+email 同批同閘門）」。收件人從「全體團隊」縮到「該寫沒寫的當事人」。
- **加開案把關**：只處理 `phase='active'`（已開案）專案；草稿不催繳。報告 PDF cron、逾期提醒同步只對已開案發。
- **呈現**：逐專案各一則（沿用 `{{projectName}}` 模板，收件人 = R 本人）。
- **註**：通知排程目前為暫停狀態（`cron.notification.enabled=false`），邏輯已修對，重啟排程後即為正確行為。

<details><summary>原始發現（保留備查）</summary>
- **位置**：`app/api/cron/weekly-notification/route.ts:211-214`
- **現象**：cron 用 `prisma.weeklyUpdate.count({...})` 判斷「本週是否有上傳」。但 A 送週報寫的是 `TaskLog`+`WeeklyReportNote`，R 寫的是 `TaskLog`/`MemberWeeklyReport`。`WeeklyUpdate` 只由 `app/api/weekly-updates/route.ts` 建立，而該端點**前端無人呼叫**。
- **後果**：`updateCount` 幾乎永遠為 0 → cron 對**所有專案**發「週報尚未上傳」通知與 email，即使本週已填。假陽性催繳洗版。
- **建議**：把「本週是否有上傳」改為查真實模型：本週的 `TaskLog`（有 `publishedAt` 或 A 的 `weekOf=本週`）或 `WeeklyReportNote(submittedAt 本週)`。
</details>

### #4 里程碑進度雙來源、會來回跳 ✅ 已修（2026-07-09）
- **模型確認（需求方）**：每個任務(含父層)進度 = 它自己壓的人的報告(報告日期推算)；**只有里程碑做聚合，聚合基準 = 葉任務(最底層原子工作)**；父任務自己的報告只顯示在它自己的甘特條、不重複計進里程碑。
- **已處理**：`app/api/projects/[id]/route.ts` 與 `app/api/my-tasks/route.ts` 的行內里程碑同步，從「只取頂層父任務 `!t.parentId`」改為「葉任務(不在 parentIds 集合者)」，與 `syncMilestoneStatus` / 甘特 `msLeaves` 三方一致 → 單一來源，不再跳。
- **例**：M{ 父P(10天,40%)[子C1(5天,100%)、子C2(5天,0%)] } → 里程碑 = (100×5+0×5)/10 = **50%**(不含 P 自己的 40%)，三個入口與甘特都是 50%。

### #5 延期 cascade ✅ 已修（2026-07-10，重做成明確連動模型）— 見 [[delay-cascade-model]]
- **最終模型（客戶確認）**：延任務 X →
  - 🔴 **延期群組(延長)** = `X + X 的父層鏈 + X 的里程碑`（只改 endDate；祖先連 durationDays 一起延長）。
  - 🟠 **下游(順延)** = 同里程碑內排在 X 頂層之後的任務 + 其後所有里程碑及其全部任務（改 startDate+endDate）。
  - ⚪ **不動**：X 的子孫、上游。
  - **順延依「里程碑順序 + 任務排序」**（不看相依線，因專案是手動建、沒相依）。
- **修法**：`submitDelay` 在送出時就把整批算好放進 `pendingTaskChanges`（延長群組送 `{endDate,durationDays}`、順延送 `{startDate,endDate}`）+ `affectedMilestones`（X 里程碑延長 + 其後順延）。核准端 step 2/2c 套用；視覺用「有沒有 startDate」判斷紅/琥珀。
- **重要脈絡**：這是**延期專用連動**，板上/編輯專案的預計日期照舊**手動、不連動、以 DB 為準**（client 把相依當 bug 硬吵掉）。
- **舊的 dependency-based `delayDownstream` 已移除**；step-4 迴圈保留未動（不可移除 cascade 約束遵守）。

<details><summary>原始發現（保留備查）</summary>
- **位置**：`app/api/delay-requests/[id]/review/route.ts:236-250`
- **現象**：核准後里程碑 dueDate 有改（159-164）、trigger 任務 duration 有加（191-210），但重排迴圈內 `taskStart = new Date(task.startDate)`（複製現有起始）、`taskEnd = addDays(taskStart, dur-1)`，**沒有把後續任務接到前一個結束的隔天**（無 `taskCurrent = addDays(taskEnd,1)` 串接），算出的 `msStart` 也**完全沒被使用**。除非任務 endDate 本來就與「startDate+duration-1」不一致，否則 if 條件為 false，什麼都不更新。
- **後果**：延期核准後，後續任務/子任務日期**並未真正順延**，里程碑連鎖順延也幾乎不會發生。cascade 名存實亡。
- **需你確認**：這塊是「不可移除 cascade」的保護區。要修的話我會先出「修改方案 + 影響範圍」給你看再動。**先確認：延期核准後，後續任務是否本來就該自動順延？**（若貴公司流程是手動調，那這反而不是 bug。）
</details>

---

## 🟠 P1 — 正確性 / 顯示不一致

### #6 三套「今天」定義，逾期/狀態差一天 ✅ 已修（2026-07-10）
- **修法**：`gantt-chart.tsx:154`（今天線 + 逾期橘條 `today > plannedEnd`）與 `milestone-task-view.tsx:271,277`（逾期/無活動判定）的 `new Date()` 全改為 `lib/date-utils` 的 `todayUtc()`。四方（server / 甘特 / 里程碑任務視圖 / DB）同一套「今天」= UTC 日曆日、不含時刻 → 逾期不再差一天。
- **未動（屬 #P5）**：甘特 `todayPct` 的位置計算仍有 UTC/本地基準混用（純視覺、次像素～日級位移），另列 P2 #P5。

### #7 `reportOnly` 型別 / 註解 / schema 三方矛盾 ✅ 決策已定（行為已正確，剩死碼清理→延後）
- **決策（2026-07-10 客戶確認）**：**每筆 A 報告都有日期 → 都驅動進度**，不存在「純敘述不推進度」。所以 `reportOnly` 概念作廢。
- **現況**：進度計算**已經**是「所有有日期的報告都驅動」（`sync-milestone-status` 早已不讀 reportOnly）→ **行為正確、非功能 bug**。
- **殘留死碼（延後清）**：`weekly-report/route.ts:104` 仍寫 `reportOnly:true`（沒人讀）、`sync-milestone-status.ts:203` 型別宣告未用、`prisma/schema.prisma:339` 欄位 + 誤導註解。清理要 `db push`（schema 改動），歸入「流程穩定後再清」。

### #8 R 報告沒帶 weekOf，A 依工作日而非填報週看到 ✅ 已修（2026-07-10）
- **實際根因（查證後，比原稽核更根本）**：R 對話框雖有週別選擇器 `rReportWeekOf`，但**送出 `task-logs/batch` 沒帶 weekOf、route 也沒存**，所以 R 紀錄只有工作日(logDate)、沒有填報週(weekOf)。而 A 撰寫台「查看 R 報告」(`rLogsOf`) 是**用 logDate 範圍篩** → R 補填上週工作、A 這週就看不到。
- **修法（三步貫通 weekOf）**：① my-tasks R 對話框送出帶 `weekOf: rReportWeekOf`；② `task-logs/batch` route 建立/更新時存 `weekOf`；③ 撰寫台 `rLogsOf` 改**依 weekOf 篩**（舊資料無 weekOf 則 fallback logDate）。→ R 填 W12 的報告，A 在 W12 就看得到。
- **註**：原稽核提的 publishLogs 路徑因 #1 撤回（R 不 publish）已無關；更新紀錄的 A 已發布報告本來就帶 weekOf（weekly-report POST 有設），分組正確。

### #9 逾期任務進度顯示 0 ✅ 已修（2026-07-10，客戶決策：有紀錄就有進度）
- **決策**：不管快逾期/已延期，**有報告就算進度**。
- **修法**：`sync-milestone-status.ts` 拿掉「只算截止日前紀錄」的過濾（`logsUpToEnd = allLogs`）。逾期報告日超過 endDate → 分子>分母 → 封頂 99%（只有 completedAt 才 100）。有紀錄的任務不再顯示 0。

### #10 父任務「有 % 卻沒實際條」 ✅ 已解（被「任務進度以自己報告為主」新模型順手解掉）
- **現況**：`displayProgress`(顯示%) = `task.progress`、實際條 = `earliestLogDateMap.has(task.id)`，**兩者現在都來自「父任務自己的 log」**（GET 時 `syncTaskProgressFromLogs` 讓每個任務進度=自己報告，父沒寫就 0）。→ 有 % 必有自己的 log 必畫得出實際條，一致。
- **註**：原稽核基於「父 % 來自子層 rollup」的舊狀態；現在父進度以自己報告為主，此不一致已消失。

### #11 延期紅段依賴會被清掉的 original 欄位 ✅ 已修（2026-07-09）
- **修法**：(a) 核准延期套用 `pendingTaskChanges` 時，`review/route.ts` step 2c **保存 `originalStartDate/EndDate`**（僅首次），讓甘特有紅段資料；(b) `repairTaskDates` 的「非重疊即清 original」廣域清理，**跳過「有已核准延期」的任務**（由 GET 從 `project.delayRequests` status='approved' 的 `taskId` + `pendingTaskChanges.taskId` 組成保護集合傳入）。
- **註**：`repairTaskDates` 的主重排迴圈只動「壞掉的日期」，不會把延期後的有效日期重排回去（已確認），故延期後的日期安全。
- **未改**：`tasks/[taskId]/route.ts:117-120` 手動改日期時仍清 original（合理：手動改就不是延期紅段了）。

### #12 同日同任務同作者，內容被靜默覆蓋 ✅ 已修（2026-07-10，A 補上同日防呆）
- **查證**：後端（A `weekly-report`、R `task-logs/batch`）都以 `taskId+authorId+logDate` 判重、同日會合併覆蓋。**UI 卡控**：R 端本來就有 `rDuplicateDates` 擋（送出 disable + 提示）；**A 撰寫台原本沒擋** → 就是客戶踩到「以為附加、結果覆蓋」的坑。
- **修法**：A 撰寫台加 `duplicateDateTasks`（同一任務多列同一天）→ 送出按鈕 disable + 提示「同一天視為同一筆會互相覆蓋，請併到同一列」。**A / R 兩端一致擋住。**
- **決策**：維持「同日=同一筆」設計（不允許同日多筆），UI 兩端明示。

### #13 側邊欄徽章讀 localStorage 假資料 ✅ 已修（2026-07-10）
- **查證**：確實還在用 `useProjectStore()`（localStorage mock），驅動側邊欄「審核」「我的任務」兩個徽章數字。
- **修法**：新增 `GET /api/sidebar-badges`（DB 真實數據：待審核=使用者為 S 的專案 pending 延期數；逾期=指派給他、未完成且 endDate<今天）。`dashboard-layout` 改 fetch 此端點，移除 `useProjectStore`/`computeTaskStatus`。

### #14 單筆壞 JSON 會 500 整支專案 API ✅ 已修（2026-07-10）
- **修法**：`lib/utils.ts` 新增 `safeJsonParse(raw, fallback)`（壞資料回 fallback + warn，不 throw）。把 `project-transformer`、`my-tasks/route`、`task-logs/route`、`task-logs/[logId]/route`、`weekly-report/route` 的 `JSON.parse(attachments/nextPlans)` 全換成它。→ 單筆壞資料只略過該筆，不再整支 API 500。

### #15 R→A 通知去重瑕疵 ✅ 已修（2026-07-10）
- **修法**：① schema 新增專屬 `NotificationType.record_uploaded`（`db push` 已同步）；② `notifyRecordUploadedToAccountable` 改用該型別，去重改「**同 A + 同專案 + 當日時間窗**」(`createdAt >= todayUtc()`)，**移除 uploaderName 子字串比對與未讀永久窗**；③ 訊息改通用「本日有成員上傳工作紀錄」，呼叫端(task-logs/batch)不再傳 uploaderName。
- **效果**：R 一天內多次/多任務上傳 → A 當天只收 1 則；不會誤命中別的通知、不會因已讀而重發。

### #16 通知收件人漏掉純指派者 ✅ 決策已定（不是 bug，維持現狀）
- **決策（2026-07-10 客戶確認）**：**指派任務只能從專案成員裡選**，所以「被指派但不在 team members」不可能發生。現有收件人=`ProjectTeamMember` 就是正確的，**不需改**。

### #17 逾期通知掛在專案 GET（讀操作有寫副作用） ✅ 已修（2026-07-10）
- **修法**：從 `projects/[id]/route.ts` GET **移除** `notifyProjectOverdueIfNeeded`（GET 不再有寫副作用）；改在 `weekly-notification` cron 的每個已開案專案迴圈裡呼叫（7 天去重不變）。
- **註**：逾期通知現與催繳同受 cron 的 enabled + 排程時間管控 → 排程暫停時逾期通知也暫停（與催繳一致，減少雜訊）；重啟排程後每週固定時間觸發。

---

## 🔒 權限（自成一類 — 目前 mock auth，無真正伺服器端驗證）

> 這塊 memory 已記（permission-drift）。屬架構級決策（要不要引入真 session / middleware），不建議零散改。以下依風險列出。

### #S1 4 支 `/api/admin/**` 破壞性回填 API 完全無 guard 🔧【建議優先補】
- **位置**：`admin/revert-backfill/route.ts:9`、`admin/backfill-original-dates/route.ts:49`、`admin/backfill-task-durations/route.ts:103`、`admin/fix-project-starts/route.ts:56`
- **現象**：handler 是 `POST()`，沒收 request、沒 `guardAdmin`（其他 admin 路由都有）。
- **後果**：任何人一個 `POST /api/admin/revert-backfill` 就會重寫全庫每個任務的日期/工期。未授權的大規模破壞。
- **建議**：立即補 `guardAdmin(request)`，或移除這些一次性維運腳本。

### #S2 `x-user-email` header 可偽造 = 人人可當 admin 🔧
- **位置**：所有 admin 路由的 `guardAdmin`（如 `admin/users/route.ts:5-10`）
- **現象**：身分只靠前端塞的 `x-user-email` header，無 session/token 佐證。`curl -H "x-user-email: <某admin>"` 即取得 admin。

### #S3 延期審核信任 body `reviewerId`，未驗證本人 🔧
- **位置**：`delay-requests/[id]/review/route.ts:15-69`（有查 reviewerId 是否 S 角色，但不驗證呼叫者=該 reviewerId）；`resolve-support` 同型
- **後果**：知道任一 S 使用者 id 即可冒名核准/駁回延期。

### #S4 幾乎所有專案級 mutation 無 server 授權 🔧
- **位置**：任務建立/改派/刪除（`tasks/route.ts`、`tasks/[taskId]/route.ts`）、里程碑（`milestones/**`）、團隊（`team/**`）、專案編輯/刪除（`projects/[id]/route.ts`）
- **現象**：前端有擋（`permissions.ts`），後端幾乎不擋。直接打 API 即可繞過。`completedBy`/`reviewActor`/`publishedBy` 由 body 自填可冒充。

### #S5 其他
- `users/[id]` PUT 可改任意人 email（身分鍵）— `users/[id]/route.ts:131`
- `users/search` 未授權洩漏全體 email/role — `users/search/route.ts:5`
- CRON 未設 secret 時預設放行 — `cron/*/route.ts`（`if (!secret) return true`）

---

## 🟡 P2 — 防禦 / 清理

- **#P1 by-name `findFirst` 解析身分（同名指到錯人）** 🔧 — `team/route.ts:63`、`projects/route.ts:405`、`notifications.ts:51`（後者已加 fallback，但仍以 name 為主）。應以 email/userId 為主鍵。
- **#P2 fire-and-forget 通知未 `.catch`** 🔧 — `tasks/route.ts:97`、`tasks/[taskId]/route.ts:166`、`delay-requests/route.ts:112,119`、`review/route.ts:109,332`。查詢在 try 外 → unhandled rejection。
- **#P3 祖先進度 rollup 無交易** 🔧 — `tasks/[taskId]/route.ts:187-219`。同父下兩子並發更新 → lost-update。
- **#P4 死碼** 🔧 — `weekly-activity-summary.tsx` 的 matrix 分支（`viewMode` 硬編 'summary' 永不執行，含「X 天未更新」提示也一起失效）；`app/gantt/page.tsx`、`app/projects/[id]/update/page.tsx` 死頁（用 useProjectStore）；`reports/pdf/route.ts.backup`、`test-budget-table`。
- **#P5 甘特 UTC/本地基準混用** 🔧 — `gantt-chart.tsx:100-156`，非 UTC+8 部署會位移。
- **#P6 週次時區工具不統一** 🔧 — 前端 `getWeekMonday`（本地）vs API `getMondayOfWeek` / `weekBounds`（UTC），週日/年初交界 off-by-one。
- **#P7 `Math.min(...allDates)` 大陣列 spread + NaN 保護** 🔧 — `gantt-chart.tsx:100-112`。
- **#P8 更新紀錄篩選依 assignee 而非 log author** 🔀 — 代填者被算到別人頭上；需定義篩選語意。

---

## 需要你「對照流程/需求」決策的清單（🔀 尚未決策）

- **#12** — 同一天允不允許同任務**多筆紀錄**（現在會靜默覆蓋）？
- **#16** — 通知收件人**是否含「純指派者(非 team member)」**？
- **#P8** — 更新紀錄人員篩選依 **log author 還是 task assignee**？

---

## 建議動工順序（依「先修流程可見 bug、權限/清理延後」策略）

1. **使用者看得到的 bug（優先清）**：**#14**（壞 JSON 拖垮整支 API）+ **#13**（側邊欄假徽章）+ **#15**（通知去重）+ **#17**（逾期通知移出 GET）。
2. **🔀 決策題**（#9 逾期顯示0要不要給底 / #16 / #P8）— 你確認後我再動。
3. **延後（流程穩定後再處理）**：**#S1~S5** 權限、**P2** 清理（#P1~P7、死碼）、**#7 reportOnly 死碼**。
